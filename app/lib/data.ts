

export async function fetchUsers(user_id?: String) {
    // fetch all users if no user_id argument passed in,
    // otherwise, fetch only the user that matches the user_id
    return [];
}

export async function fetchBioPages(bioPage_id?: String) {
    // fetch all bioPages if no bioPage_id argument passed in,
    // otherwise, fetch only the bioPages that matches the bioPage_id
    return [];
}